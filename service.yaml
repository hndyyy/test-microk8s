apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: test-k8s
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort  # Bisa diganti jadi ClusterIP atau LoadBalancer sesuai kebutuhan
